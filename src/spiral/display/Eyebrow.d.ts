import * as React from 'react';
/** Small uppercase mono label: slide section names, panel headings, field captions. */
export interface EyebrowProps { children?: React.ReactNode; color?: 'accent' | 'blue' | 'ink' | 'muted' | string; style?: React.CSSProperties; }
export declare function Eyebrow(props: EyebrowProps): JSX.Element;
