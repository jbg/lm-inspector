import * as React from 'react';
/** The pixel-tile spiral logo (assets/spiral-mark.png). Pass src when the asset path differs in your project. */
export interface SpiralMarkProps { size?: number; src?: string; style?: React.CSSProperties; }
export declare function SpiralMark(props: SpiralMarkProps): JSX.Element;
